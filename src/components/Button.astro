---
interface Props {
  type: string;
  text: string;
  anchor?: string;
}

const { type, text, anchor } = Astro.props;
---

<a
  href={`#${anchor && anchor}`}
  class={`btn ${type === 'secondary' && 'secondary'}`}>{text}</a
>

<style>
  .btn {
    padding: 0.2rem 1.6rem;
    border-radius: 100px;
    background-color: var(--button-color);
    text-align: center;
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--color-bg);
    border: solid 1px #212529;
    transition: 0.3s;
    position: relative;
  }

  .btn:hover:not(.secondary) {
    box-shadow: 5px 6px 8px 0 rgba(0, 0, 0, 0.24),
      0 8px 25px 0 rgba(0, 0, 0, 0.15);
  }

  .btn:active:not(.secondary) {
    transform: scale(0.9);
  }
  .secondary {
    color: var(--button-color);
    background-color: transparent;
    border: none;
  }

  @media (max-width: 1075px) {
    .btn {
      padding: 0.2rem 1.6rem;
    }
  }
</style>
